<h2> All Books </h2>

<div class="next-page">
  <%= will_paginate %>
</div>
<br>
<ul class="flex-container">
  <% @books.each do |book| %>
  <li class="book_box"><%= link_to book.name, book_path(book)%></li>
  <br>
  <% end %>
</ul>
